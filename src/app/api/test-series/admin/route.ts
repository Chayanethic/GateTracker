import { NextResponse } from 'next/server';
import { createClient } from '@supabase/supabase-js';
const db=()=>createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!);
function ok(req:Request){return req.headers.get('x-admin-email')===process.env.NEXT_PUBLIC_ADMIN_EMAIL&&req.headers.get('x-admin-password')===process.env.NEXT_PUBLIC_ADMIN_PASSWORD;}
export async function GET(req:Request){if(!ok(req))return NextResponse.json({error:'Unauthorized'},{status:403});const [{data:requests,error:e1},{data:tests,error:e2}]=await Promise.all([db().from('test_series_access_requests').select('*').order('requested_at',{ascending:false}),db().from('test_series').select('id,title,exam_name,duration_minutes,max_marks,question_count,created_at').order('created_at',{ascending:false})]);if(e1||e2)return NextResponse.json({error:(e1||e2)?.message},{status:500});return NextResponse.json({requests:requests||[],tests:tests||[]});}
export async function PATCH(req:Request){if(!ok(req))return NextResponse.json({error:'Unauthorized'},{status:403});const {id,status}=await req.json();if(!id||!['approved','rejected'].includes(status))return NextResponse.json({error:'Invalid request'},{status:400});const {error}=await db().from('test_series_access_requests').update({status,reviewed_at:new Date().toISOString()}).eq('id',id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
